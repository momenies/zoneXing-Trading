# تشغيل zoneXing Trading على سيرفر (دليل النشر)

> **اقرأ التحذير أولاً:** ملف الباك تست الأصلي `zoneXing_Trading_signal_engine.py`
> يستخدم بيانات مستقبلية (look-ahead) في `_fractal_pivots` — راجع قسم
> [نتائج الباك تست](#لماذا-لن-تتكرر-نتائج-الباك-تست) قبل أن تخاطر بأي مبلغ.

---

## 1. المتطلبات

- سيرفر Linux (VPS صغير يكفي: 1 vCPU / 1GB RAM) — يفضّل قريب جغرافياً من المنصة.
- Python 3.10+ أو Docker.
- ساعة السيرفر مضبوطة على UTC ومزامَنة (`timedatectl set-ntp true`) — البوت يعمل
  على إغلاق شمعة 5 دقائق، وانحراف الوقت يعني دخول خاطئ.
- حساب منصة يدعم ccxt. للبيع على المكشوف (short) تحتاج **عقود دائمة USDT (swap)**؛
  السبوت يسمح بالشراء فقط والبوت يتجاهل إشارات البيع تلقائياً.

---

## 2. التنصيب

### أ) Docker (الأسهل والموصى به)

```bash
git clone <repo-url> zonexing-trading && cd zonexing-trading
cp .env.example .env && nano .env        # اضبط الإعدادات (ابدأ MODE=paper)
docker compose up -d --build
docker compose logs -f
```

### ب) systemd (بدون Docker)

```bash
sudo useradd -r -m -d /opt/zonexing-trading zonexing
sudo git clone <repo-url> /opt/zonexing-trading
cd /opt/zonexing-trading
sudo -u zonexing python3 -m venv .venv
sudo -u zonexing .venv/bin/pip install -r requirements.txt
sudo -u zonexing cp .env.example .env && sudo -u zonexing nano .env
sudo chmod 600 .env

sudo cp deploy/zonexing.service /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now zonexing
journalctl -u zonexing -f
```

---

## 3. اختبر قبل ما تشغّل بفلوس

```bash
python -m live.trader --selftest   # فحص السببية + قيود الاستراتيجية (بدون إنترنت)
python -m tests.test_live          # اختبارات الوحدة
python -m live.demo                # تشغيل كامل على بيانات تجريبية (يفتح صفقات وهمية)
python -m live.trader --once       # دورة واحدة على أسعار المنصة الحقيقية (paper)
```

`--selftest` يعمل تلقائياً داخل بناء Docker: لو الإشارة صارت غير سببية البناء يفشل.

---

## 4. التدرّج إلى التشغيل الحقيقي

**لا تقفز مباشرة إلى `MODE=live`.** الترتيب الصحيح:

| المرحلة | الإعداد | المدة المقترحة |
|---|---|---|
| 1. ورقي | `MODE=paper` | 5–7 أيام كاملة |
| 2. تجريبي بالمنصة | `MODE=live` + `TESTNET=true` | 3–5 أيام |
| 3. حقيقي مصغّر | `MODE=live`, `INVEST_FRAC=0.05`, `LEVERAGE=1` | أسبوعان |
| 4. الحجم الكامل | ارفع `INVEST_FRAC` تدريجياً | بعد مراجعة `state/trades.csv` |

لتفعيل الأوامر الحقيقية لازم **ثلاثة** شروط معاً، وإلا يرفض البوت الإقلاع:

```ini
MODE=live
API_KEY=...   /   API_SECRET=...   (و API_PASSWORD لمنصات OKX/KuCoin)
I_UNDERSTAND_LIVE_RISK=yes
```

**مفاتيح الـAPI:** فعّل صلاحية التداول فقط — **بدون صلاحية سحب**، وقيّدها على
IP السيرفر. الملف `.env` مستثنى من git ويجب أن يكون `chmod 600`.

---

## 5. أوامر التشغيل اليومية

```bash
python -m live.trader             # الحلقة المستمرة
python -m live.trader --once      # دورة واحدة
python -m live.trader --status    # الحالة والصفقات المفتوحة
python -m live.trader --flatten   # إغلاق كل الصفقات فوراً (زر الطوارئ)
```

الملفات الناتجة في `state/`:

| الملف | المحتوى |
|---|---|
| `state.json` | المراكز المفتوحة والتبريد وحد الخسارة اليومي (يُستعاد بعد إعادة التشغيل) |
| `trades.csv` | سجل كل صفقة مغلقة: الدخول، الخروج، السبب، الربح/الخسارة |
| `zonexing.log` | سجل التشغيل الكامل |

**الاسترجاع بعد إعادة التشغيل:** عند الإقلاع يقرأ البوت `state.json` ثم يطابقه مع
مراكز المنصة الفعلية (`--live` فقط): مركز في الملف وغير موجود لدى المنصة يُمسح،
ومركز موجود لدى المنصة وغير مسجّل يُتبنّى حتى تُدار مخارجه.

---

## 6. حواجز الأمان المدمجة

- **البوابة (BTC) لا تُتداول أبداً** — مرفوضة في التحقق من الإعداد ومتجاهَلة في الحلقة.
- **لا مركز يعاكس البوابة** — فحص قبل كل دخول + خروج إجباري عند انقلاب البوابة.
- **أوامر حماية على المنصة** (`PROTECTIVE_ORDERS=true`, عقود فقط): وقف خسارة وجني
  أرباح `reduce-only` تُرسل فور الدخول، فلا تهرب الصفقة لو انقطع السيرفر.
- **`MAX_DAILY_LOSS_PCT`** يوقف الدخولات الجديدة لبقية اليوم (UTC) — المخارج تبقى شغالة.
- **`MAX_CONSECUTIVE_LOSSES`** يوقف الدخولات حتى إعادة التشغيل اليدوية.
- **`MAX_OPEN_POSITIONS`** سقف عدد المراكز المتزامنة.
- **إيقاف نظيف** على `SIGTERM/SIGINT`، مع `CLOSE_ON_EXIT=true` اختيارياً لتصفية كل شيء.
- **تنبيهات تيليجرام** اختيارية (`TELEGRAM_TOKEN` + `TELEGRAM_CHAT_ID`).

---

## 7. لماذا لن تتكرر نتائج الباك تست

في `zoneXing_Trading_signal_engine.py`:

```python
for i in range(p, n - p):
    window_lo = lo[i - p: i + p + 1]     # ← 8 شمعات مستقبلية
    dip[i] = lo[i] == window_lo.min()
```

قرار الدخول عند الشمعة `i` يعتمد على شمعات لم تُطبع بعد (40 دقيقة في المستقبل).
فحص `--selftest` يقيس الأثر: **255 إشارة قاع/قمة** لم تكن معروفة في وقتها، و**16 من
كل 300 إشارة لحظية** تختلف عن إشارة "ما بعد الحدث" على نفس البيانات.

لذلك أرقام `+163%` و `PF 6.23` و `DD 1.94%` **لا تمثل** ما سيحدث لايف.

المحرّك اللايف (`live/engine.py`) يحتفظ بكل قواعدك الأخرى بدون تغيير — البوابة،
تأكيد EMA، وقف ATR المتحرك، TP/SL الثابتة، الخروج عند انقلاب البوابة، الحد الأدنى
للاحتفاظ، التبريد — ويستبدل الكاشف فقط بواحد سببي:

| `PIVOT_MODE` | الوصف | التأخير |
|---|---|---|
| `donchian` (افتراضي) | قاع = أدنى سعر خلال آخر `2*p+1` شمعة — وهي دالة `_donchian_pivots` الموجودة أصلاً في ملفك وغير المستخدمة | صفر |
| `fractal_confirmed` | نفس الفراكتال الأصلي لكن يُنفَّذ بعد `p_bars` شمعات عند اكتمال النافذة | 8 شمعات (40 دقيقة) |

**الخطوة المطلوبة منك:** أعد الباك تست باستخدام `_donchian_pivots` بدل
`_fractal_pivots` لتعرف التوقع الحقيقي قبل التشغيل. الرقم الناتج هو مرجعك، وليس
الأرقام المنشورة في README.

---

## 8. ملاحظات تكلفة حقيقية

- استراتيجية 5 دقائق = دوران عالٍ: رسوم التيكر + الانزلاق هما العدو الأول.
  الافتراضات هنا `TAKER_RATE=0.0005` و `SLIPPAGE=0.0005` — تحقّق من شريحة رسومك.
- الصفقات القصيرة على العقود الدائمة تدفع/تقبض **تمويلاً** كل 8 ساعات، وهذا غير
  محسوب في الباك تست.
- بعملاء صغار قد ترفض المنصة الحجم: البوت يوقف الصفقة برسالة واضحة إذا نزل الحجم
  عن الحد الأدنى بدل أن يرسل أمراً فاشلاً.
